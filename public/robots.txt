User-agent: *
Allow: /

# /api/sitemap is the only API route and it serves this file, so nothing under
# /api is disallowed, so the previously submitted /api/sitemap URL keeps working.
Sitemap: https://www.ouaknine-avocats.com/sitemap.xml
